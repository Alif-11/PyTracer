import math
import numpy as np

from vector import Vector
from color import Color
from objects import Sphere

class Intersection:

  @staticmethod
  def nearest_intersection(list_of_intersections):
    """
    Returns the nearest intersection between a ray and an object."""
    pass